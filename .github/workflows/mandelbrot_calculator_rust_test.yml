name: mandelbrot_calculator_rust_test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: cd
      run: cd mandelbrot_calculator_rust
    - name: Run tests
      run: bazel test //...
