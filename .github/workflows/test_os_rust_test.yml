name: test_os_rust_test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Run tests
      run: | 
        rustup install nightly
        rustup set nightly
        rustup target add x86_64-test_os.json
        cd test_os_rust
        cargo test --verbose
